org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
# Note: custom buildDir was previously set to work around spaces-in-path issues.
# Commented out because flutter run cannot find the APK when output is outside project dir.
# org.gradle.project.buildDir=C:/FlutterBuild/Smart-Parcel-Dropbox/${project.name}/build
