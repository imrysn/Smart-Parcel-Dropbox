/// Google Auth Service for Smart Parcel Drop Box System
/// Stubbed out version (previously used Firebase Authentication)
class GoogleAuthService {
  /// Sign in with Google (Disabled for MongoDB-only version)
  Future<void> signInWithGoogle() async {
    throw 'Google Sign-In is not supported in the current version. Please use email and password.';
  }

  /// Sign out from Google
  Future<void> signOut() async {
    // No-op
  }
}
